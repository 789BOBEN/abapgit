CLASS lhc_ZI_rap_abook_0309 DEFINITION INHERITING FROM cl_abap_behavior_handler.
  PRIVATE SECTION.

    METHODS get_instance_authorizations FOR INSTANCE AUTHORIZATION
      IMPORTING keys REQUEST requested_authorizations FOR ZI_rap_abook_0309 RESULT result.

    METHODS get_global_authorizations FOR GLOBAL AUTHORIZATION
      IMPORTING REQUEST requested_authorizations FOR ZI_rap_abook_0309 RESULT result.
    METHODS set_creation_date FOR DETERMINE ON MODIFY
      IMPORTING keys FOR ZI_rap_abook_0309~set_creation_date.

    METHODS validate_book FOR VALIDATE ON SAVE
      IMPORTING keys FOR ZI_rap_abook_0309~validate_book.
    METHODS approve FOR MODIFY
      IMPORTING keys FOR ACTION ZI_rap_abook_0309~approve RESULT result.

ENDCLASS.

CLASS lhc_ZI_rap_abook_0309 IMPLEMENTATION.

  METHOD get_instance_authorizations.
  ENDMETHOD.

  METHOD get_global_authorizations.
  ENDMETHOD.

  METHOD set_creation_date.
    " 设置创建时间
    MODIFY ENTITIES OF ZI_rap_abook_0309 IN LOCAL MODE
      ENTITY ZI_rap_abook_0309
      UPDATE
      FIELDS ( booking_date )
      WITH VALUE #( FOR key IN keys
        ( %tky = key-%tky
          booking_date = '20290909' ) )
    MAPPED DATA(mapped_update)
    FAILED DATA(failed_update)
    REPORTED DATA(reported_update).

  ENDMETHOD.

  METHOD validate_book.
  ENDMETHOD.

  METHOD approve.
  " 在某个类或程序中调用
DATA: lt_keys  TYPE TABLE of ZI_rap_abook_0309,
    lt_result TYPE TABLE FOR READ RESULT ZI_rap_abook_0309.


.


    LOOP AT keys INTO DATA(key1).
         SELECT FROM ZI_rap_abook_0309 as aa
         FIELDS *
         where aa~booking_uuid = @key1-booking_uuid
         into TABLE @DATA(lt_tab).

    ENDLOOP.
  ENDMETHOD.

ENDCLASS.
